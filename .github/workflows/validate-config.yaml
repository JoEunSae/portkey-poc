name: Validate Gateway Config

on:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Display Config
        run: |
          cat gateway-config.yaml

      - name: Validation Passed
        run: |
          echo "Gateway Config Validation Passed"
